% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{html_parse}
\alias{html_parse}
\alias{html_read}
\title{Parse HTML}
\usage{
html_parse(
  x,
  encoding = NULL,
  base_url = NULL,
  comments = TRUE,
  limits = html_limits()
)

html_read(path, ...)
}
\arguments{
\item{x}{One string, or a raw vector of encoded bytes.}

\item{encoding}{The encoding of raw input, as a name \code{\link[=iconv]{iconv()}} accepts;
\code{NULL} to use a byte-order mark, the page's declaration, or UTF-8.}

\item{base_url}{The document's URL, used to resolve relative links; \code{NULL}
if unknown.}

\item{comments}{Whether to keep comment nodes.}

\item{limits}{Resource limits from \code{\link[=html_limits]{html_limits()}}.}

\item{path}{One file path, URL or connection; see "Reading files, URLs
and connections".}

\item{...}{Arguments passed on to \code{html_parse()}.}
}
\value{
A \code{zuhtml_document}.
}
\description{
\code{html_parse()} parses one string or raw vector of HTML the way a browser
does: omitted end tags, unquoted attributes, character references and
misnested elements are repaired by the HTML parsing algorithm, never
rejected. \code{html_read()} reads and parses one file, URL or connection.
}
\details{
\code{html_parse()} never treats a string as a file name or URL.
}
\section{Reading files, URLs and connections}{

\code{html_read()} reads its input as raw bytes, then decodes and parses them
as \code{html_parse()} does:
\itemize{
\item a string with an \code{http}, \code{https}, \code{ftp}, \code{ftps} or \code{file} scheme (in
any case) is read with \code{\link[=url]{url()}}, and becomes the document's \code{base_url}
unless \code{base_url} is given. A redirect is not seen, so after one the
base URL is the address asked for. HTTP headers are not read either:
the encoding comes from a byte-order mark, \code{encoding} or the page's
\verb{<meta>}. For anything more (headers, authentication, retries), fetch
with an HTTP client and pass the body to \code{html_parse()};
\item any other string is a file path;
\item a connection, such as \code{\link[=gzfile]{gzfile()}} or \code{\link[=rawConnection]{rawConnection()}}, is read as
\code{\link[=readBin]{readBin()}} reads one: an unopened connection is opened in \code{"rb"} mode
and closed afterwards; an open one must be in binary mode, is read
from its current position, and is left open. A non-blocking pipe or
socket that has no data yet is an error rather than a short read.
}

Reading stops with a \code{zuhtml_limit_error} as soon as the input passes
four times \code{max_input} bytes, before a larger file is read at all. A
file that does not exist, an unreachable URL, and a connection that
cannot be read are \code{zuhtml_input_error}s. The input is always read
whole before parsing, because decoding needs all of it.
}

\section{Encoding}{

A character string is taken as text: it is converted to UTF-8 with
\code{\link[=enc2utf8]{enc2utf8()}}, and \code{encoding} must be \code{NULL} or \code{"UTF-8"}. A string marked
as \code{"bytes"} is rejected; pass a raw vector instead.

A raw vector is decoded with, in order of precedence, a byte-order mark
(UTF-8, UTF-16LE or UTF-16BE), \code{encoding}, a declaration in the page, or
UTF-8. A byte-order mark that contradicts \code{encoding} is an error, and so
is any byte sequence that is invalid in the chosen encoding: nothing is
replaced silently. A fetcher that knows the HTTP charset should pass it
as \code{encoding}.

The declaration is found as browsers find it, by the HTML standard's
prescan of the first 1024 bytes for \verb{<meta charset="...">} or
\verb{<meta http-equiv="Content-Type" content="...; charset=...">}. The
prescan skips comments and the insides of tags, but not the text of
scripts. Labels are those of the Encoding Standard, which maps several
to a superset: \code{"iso-8859-1"}, \code{"latin1"} and \code{"us-ascii"} mean
windows-1252, \code{"gb2312"} means GBK, and a UTF-16 label means UTF-8 (the
bytes read as ASCII, so they are not UTF-16). An unknown label is
ignored. \code{\link[=html_info]{html_info()}} reports the encoding used and its source.

A file saved in another encoding without updating its declaration, as
some tools do when they convert pages to UTF-8, decodes wrongly or fails
to decode, as it would in a browser. Pass its real encoding as
\code{encoding}.

A leading byte-order mark is removed. Input containing a NUL character
after decoding is rejected.
}

\examples{
doc <- html_parse("<p>Hello <b>world</b>")
doc

# Raw bytes in a declared encoding:
html_parse(as.raw(c(0x3c, 0x70, 0x3e, 0xe9)), encoding = "latin1")

# From a file:
path <- tempfile(fileext = ".html")
writeLines("<title>Saved page</title><p>Text", path)
html_read(path)

# From a compressed file, through a connection:
gz <- tempfile(fileext = ".html.gz")
writeLines("<p>Compressed", gzfile(gz))
html_read(gzfile(gz))

# From a URL, when online; relative links resolve against it:
if (interactive()) {
  doc <- html_read("https://cran.r-project.org/web/packages/")
  head(html_links(doc, absolute = TRUE))
}
}
\seealso{
\code{\link[=html_problems]{html_problems()}} for the parse errors that were repaired;
\code{\link[=html_limits]{html_limits()}}; \link{zuhtml-conditions} for the errors these functions
raise.

Other parsing:
\code{\link[=html_fragment]{html_fragment()}},
\code{\link[=html_info]{html_info()}},
\code{\link[=html_limits]{html_limits()}},
\code{\link[=html_problems]{html_problems()}},
\code{\link[=zuhtml_info]{zuhtml_info()}}
}
\concept{parsing}

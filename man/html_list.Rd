% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list.R
\name{html_list}
\alias{html_list}
\title{Extract an HTML list}
\usage{
html_list(x, mode = c("text", "tree"))
}
\arguments{
\item{x}{A \code{zuhtml_nodeset} holding exactly one \verb{<ul>} or \verb{<ol>} element,
as from \code{\link[=html_element]{html_element()}}.}

\item{mode}{\code{"text"} or \code{"tree"}.}
}
\value{
\itemize{
\item \code{"text"}: a character vector, one string per item.
\item \code{"tree"}: an object of class \code{zuhtml_list}, a list with \code{type} (\code{"ul"}
or \code{"ol"}) and \code{items}, a list with one element per item; each item is
a list with \code{text} (as in text mode) and \code{children} (a list of
\code{zuhtml_list} objects, one per list nested in the item).
}
}
\description{
Reads one \verb{<ul>} or \verb{<ol>} element. In \code{"text"} mode, the result has one
string per item: the item's cleaned text (see \code{\link[=html_text_clean]{html_text_clean()}}),
without the text of any list nested inside it, so child items do not
leak into their parent. A nested list still separates the text around
it with a line break. In \code{"tree"} mode, nested lists become children
of the item that contains them, including lists inside wrapper elements
such as \verb{<div>}.
}
\details{
Items are the \verb{<li>} children of the list, in source order; empty items
are \code{""} and duplicates are kept.
}
\examples{
doc <- html_parse("<ul><li>Apples<li>Tools<ul><li>Hammer<li>Saw</ul></ul>")
items <- html_element(doc, "ul")
html_list(items)
html_list(items, mode = "tree")
}
\seealso{
Other extraction:
\code{\link[=html_links]{html_links()}},
\code{\link[=html_table]{html_table()}},
\code{\link[=html_url]{html_url()}}
}
\concept{extraction}

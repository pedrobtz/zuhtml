% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{html_problems}
\alias{html_problems}
\title{Parse problems recorded for a document}
\usage{
html_problems(x)
}
\arguments{
\item{x}{A \code{zuhtml_document}, or a \code{zuhtml_nodeset} for the document
that owns it.}
}
\value{
A data frame with one row per problem, in input order, and
columns \code{stage} (\code{"tokenizer"} or \code{"parser"}), \code{code} (a stable
package-owned name, such as \code{"unexpected-end-tag"} or
\code{"duplicate-attr"}), \code{line} and \code{column} (1-based) and \code{byte_offset}
(0-based, into the decoded UTF-8 input). At most \code{max_errors} problems
are kept (see \code{\link[=html_limits]{html_limits()}}); attribute \code{"truncated"} is \code{TRUE} when
more occurred.
}
\description{
HTML parsing never fails on malformed markup: the parser repairs it as a
browser would. \code{html_problems()} lists what was repaired, which is useful
for diagnosing why a tree looks the way it does. Real pages nearly always
have some. This is not a conformance validator.
}
\examples{
doc <- html_parse("<p>One</div><p id=a id=b>Two")
html_problems(doc)
}
\seealso{
Other parsing:
\code{\link[=html_fragment]{html_fragment()}},
\code{\link[=html_info]{html_info()}},
\code{\link[=html_limits]{html_limits()}},
\code{\link[=html_parse]{html_parse()}},
\code{\link[=zuhtml_info]{zuhtml_info()}}
}
\concept{parsing}

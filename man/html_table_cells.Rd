% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{html_table_cells}
\alias{html_table_cells}
\title{Cells of an HTML table}
\usage{
html_table_cells(x, trim = TRUE, absolute = FALSE, limits = html_limits())
}
\arguments{
\item{x}{A nodeset holding exactly one \verb{<table>} element.}

\item{trim}{Whether to trim each cell's text; see \code{\link[=html_text_clean]{html_text_clean()}}.}

\item{absolute}{If \code{TRUE}, links are resolved with \code{\link[=html_url]{html_url()}};
otherwise they are \code{href} as written, as in \code{\link[=html_links]{html_links()}}.}

\item{limits}{Resource limits from \code{\link[=html_limits]{html_limits()}}; \code{max_table_cells}
bounds the grid, spans included.}
}
\value{
A data frame with one row per \verb{<td>} or \verb{<th>}, in grid order
(by row, then column), and columns:
\itemize{
\item \code{row}, \code{column}: the integer position of the cell's top-left slot;
\item \code{rowspan}, \code{colspan}: the integer number of rows and columns it
covers, after \code{rowspan="0"} is expanded and spans are clipped;
\item \code{section}: \code{"thead"}, \code{"tbody"} or \code{"tfoot"}; rows directly in the
table are \code{"tbody"};
\item \code{header}: \code{TRUE} for a \verb{<th>};
\item \code{text}: the cell's cleaned text, without nested tables, as in
\code{\link[=html_table]{html_table()}};
\item \code{links}: a list of character vectors, the \code{href} of each \verb{<a>} and
\verb{<area>} in the cell, outside nested tables.
}
}
\description{
One row per cell of one \verb{<table>}, with its position in the grid that
\code{\link[=html_table]{html_table()}} builds: spans are placed, clipped and checked exactly as
there, and rows are numbered in the same logical order, header rows
included. Use it when the data frame loses what you need: which cells are
headers, how far they span, or the links inside them.
}
\examples{
doc <- html_parse(paste0(
  "<table><tr><th rowspan=2>Package<th colspan=2>Links",
  "<tr><th>Home<th>Docs",
  "<tr><td>zuhtml<td><a href='https://example.org/'>site</a>",
  "<td><a href='/ref/'>ref</a> <a href='/news/'>news</a></table>"
), base_url = "https://example.org/pkg/")
cells <- html_table_cells(html_element(doc, "table"), absolute = TRUE)
cells[, c("row", "column", "rowspan", "colspan", "header", "text")]
cells$links[[7]]
}
\seealso{
Other extraction:
\code{\link[=html_forms]{html_forms()}},
\code{\link[=html_links]{html_links()}},
\code{\link[=html_list]{html_list()}},
\code{\link[=html_table]{html_table()}},
\code{\link[=html_url]{html_url()}}
}
\concept{extraction}

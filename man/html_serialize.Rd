% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{html_serialize}
\alias{html_serialize}
\title{Serialize nodes as HTML}
\usage{
html_serialize(x, outer = TRUE)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{outer}{If \code{TRUE}, each node with its own markup, like
\code{outerHTML}; if \code{FALSE}, only its contents, like \code{innerHTML}. A
document or fragment has no markup of its own, so both give its
contents.}
}
\value{
A character vector as long as \code{x}; \code{NA} for missing nodes.
}
\description{
Produces normalized HTML following the WHATWG serialization algorithm,
not the original input bytes: end tags the input left out are written,
attribute values are always double-quoted, and \code{&}, \code{<}, \code{>}, \verb{"} and
non-breaking spaces are escaped as the algorithm requires. Parsing the
result gives back the same tree, except where the HTML standard itself
does not guarantee that (for example markup that tree construction
rearranges).
}
\details{
Serialization is not sanitization: \verb{<script>} elements and
\verb{javascript:} URLs are written as they were parsed.

\code{as.character()} on a nodeset is \code{html_serialize()}.
}
\examples{
doc <- html_parse("<p class=x>One<br>Two & <b>three</p>")
html_serialize(doc)
p <- html_children(html_children(html_root(doc))[2])
html_serialize(p)
html_serialize(p, outer = FALSE)

# To write a document to a file:
path <- tempfile(fileext = ".html")
writeLines(html_serialize(doc), path)
}
\seealso{
Other node values:
\code{\link[=html_attr]{html_attr()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_text]{html_text()}},
\code{\link[=html_text_clean]{html_text_clean()}}
}
\concept{node values}

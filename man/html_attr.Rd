% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributes.R
\name{html_attr}
\alias{html_attr}
\alias{html_attrs}
\alias{html_classes}
\title{Attributes of elements}
\usage{
html_attr(x, name, default = NA_character_)

html_attrs(x)

html_classes(x)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{name}{The attribute name: a single string.}

\item{default}{The value for nodes that lack the attribute, including
nodes that are not elements: a single string, possibly \code{NA}.}
}
\value{
\itemize{
\item \code{html_attr()}: a character vector as long as \code{x}; \code{NA} for missing
nodes.
\item \code{html_attrs()}: a list as long as \code{x} of named character vectors, in
source order; \code{NA_character_} for missing nodes.
\item \code{html_classes()}: a list as long as \code{x} of character vectors of class
tokens; \code{NA_character_} for missing nodes.
}
}
\description{
\code{html_attr()} reads one attribute from every node; \code{html_attrs()} reads
all of them; \code{html_classes()} splits the \code{class} attribute into tokens.
Values are decoded (\verb{&amp;} becomes \code{&}). An attribute that is present
but empty is \code{""}, distinct from an absent one: test a boolean attribute
such as \code{disabled} by presence, \code{!is.na(html_attr(x, "disabled"))}.
}
\details{
Attribute names on HTML elements match regardless of ASCII case, as in a
browser; on SVG and MathML elements they match exactly (\code{viewBox}).
Namespaced attributes in foreign content are named with their prefix, as
in \code{"xlink:href"}. Where an element has the same attribute more than
once, the first wins, as the HTML parser decides.
}
\examples{
doc <- html_parse(
  "<a href='/x' class='btn  primary' data-id=7>Go</a><input disabled>"
)
body <- html_children(html_root(doc))[2]
nodes <- html_children(body)
html_attr(nodes, "href")
html_attr(nodes, "href", default = "")
html_attrs(nodes)
html_classes(nodes)
!is.na(html_attr(nodes, "disabled"))
}
\seealso{
Other node values:
\code{\link[=html_markdown]{html_markdown()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_serialize]{html_serialize()}},
\code{\link[=html_strings]{html_strings()}},
\code{\link[=html_text]{html_text()}},
\code{\link[=html_text_clean]{html_text_clean()}}
}
\concept{node values}

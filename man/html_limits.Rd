% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/limits.R
\name{html_limits}
\alias{html_limits}
\title{Resource limits for parsing and extraction}
\usage{
html_limits(
  max_input = 16 * 1024^2,
  max_memory = 512 * 1024^2,
  max_depth = 512,
  max_nodes = 4e+06,
  max_errors = 100,
  max_table_cells = 1e+06,
  max_selector_length = 16 * 1024
)
}
\arguments{
\item{max_input}{Largest input, in bytes of UTF-8 after decoding.}

\item{max_memory}{Largest native memory the parser may hold at once, in
bytes.}

\item{max_depth}{Deepest nesting of open elements. Parsing time grows
with the square of nesting depth, so this bounds it; it is enforced
while parsing, not afterwards.}

\item{max_nodes}{Most nodes a document may have.}

\item{max_errors}{Most parse problems kept for \code{\link[=html_problems]{html_problems()}}. Parsing
continues past it; only the record is truncated.}

\item{max_table_cells}{Most cells a table may expand to, spans included.}

\item{max_selector_length}{Longest CSS selector, in bytes.}
}
\value{
An object of class \code{zuhtml_limits}: a named list of the limits,
as doubles.
}
\description{
Every parse and extraction runs under explicit limits, so that untrusted
HTML cannot exhaust memory or time. Limits are per call, not global
options: pass the result to the \code{limits} argument of \code{\link[=html_parse]{html_parse()}} and
the extraction functions. Exceeding one raises a \code{zuhtml_limit_error}
(see \link{zuhtml-conditions}) after all native memory is released.
}
\details{
The memory limit is the real guard; the input limit is a cheap check
before parsing starts. Parsing and conversion together need about 26
bytes of native memory per input byte on ordinary markup, and 40 to 90 on
markup dense with small elements, tables or formatting, so a large enough
page reaches \code{max_memory} before \code{max_input} or \code{max_nodes}: a
\code{zuhtml_limit_error}, not a crash. At the defaults, 16 MiB of ordinary
markup (about 1.5 million nodes, 450 MB) parses.
}
\examples{
html_limits()

# A tighter nesting limit for a service parsing untrusted pages:
lim <- html_limits(max_depth = 128)
try(html_parse(strrep("<div>", 200), limits = lim))
}
\seealso{
Other parsing:
\code{\link[=html_fragment]{html_fragment()}},
\code{\link[=html_info]{html_info()}},
\code{\link[=html_parse]{html_parse()}},
\code{\link[=html_problems]{html_problems()}},
\code{\link[=zuhtml_info]{zuhtml_info()}}
}
\concept{parsing}

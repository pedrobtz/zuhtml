% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms.R
\name{html_forms}
\alias{html_forms}
\title{Forms and their controls}
\usage{
html_forms(x)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}
}
\value{
A list with one element per form, in document order, each a
list of:
\itemize{
\item \code{action}: the \code{action} attribute resolved as \code{\link[=html_url]{html_url()}} resolves
URLs; without one, the document's base URL, as browsers submit to
the page itself; \code{NA} when neither resolves;
\item \code{method}: \code{"get"}, \code{"post"} or \code{"dialog"} (\code{"get"} when missing or
invalid);
\item \code{enctype}: \code{"application/x-www-form-urlencoded"} (the default),
\code{"multipart/form-data"} or \code{"text/plain"};
\item \code{id}, \code{name}: the attributes, or \code{NA};
\item \code{fields}: a data frame with one row per control and columns \code{name}
(\code{NA} when absent; repeated names are kept), \code{type}, \code{value},
\code{checked} (\code{TRUE}/\code{FALSE} for checkboxes and radio buttons, \code{NA}
otherwise), \code{disabled}, and \code{options}, a list-column holding for
each select a data frame of its options (\code{value}, \code{label},
\code{selected}, \code{disabled}) and \code{NULL} for other controls.
}
}
\description{
Describes each \verb{<form>} below the given nodes (and the nodes themselves)
and the controls it owns, for inspection. Nothing is submitted, fetched
or built into a request.
}
\details{
A control belongs to a form as the HTML standard's form owner says:
the form named by its \code{form} attribute (the element with that ID, if it
is a \verb{<form>}; no form otherwise), else its nearest ancestor \verb{<form>}.
Old pages often write \verb{<table><form><tr><td><input ...>}, where the
parser closes the form at once but browsers still associate the
following controls with it; such a form, left empty inside a table,
owns the controls without another owner that follow it in that table,
up to the next form.

Controls are \verb{<input>}, \verb{<select>}, \verb{<textarea>} and \verb{<button>}
elements, in document order. Values follow the DOM:
\itemize{
\item \code{type} is an input's \code{type} (lowercased; \code{"text"} when missing or
unknown), \code{"select-one"} or \code{"select-multiple"}, \code{"textarea"}, or a
button's \code{type} (\code{"submit"} by default);
\item \code{value} is the \code{value} attribute (\code{"on"} for a checkbox or radio
button without one, else \code{""}); a textarea's text; or a select's
first selected option's value, where a single select with no option
marked selected selects its first enabled option;
\item \code{disabled} is \code{TRUE} for a control with a \code{disabled} attribute or
inside a disabled \verb{<fieldset>} (except in its first \verb{<legend>}).
}
}
\examples{
doc <- html_parse(paste0(
  "<form action='/search' method=POST>",
  "<input name=q value='zuhtml'>",
  "<select name=sort><option>relevance<option selected>date</select>",
  "<label><input type=checkbox name=exact checked> Exact</label>",
  "<button>Search</button></form>",
  "<input form=search-options name=page value=2>",
  "<form id=search-options></form>"
), base_url = "https://example.org/")
forms <- html_forms(doc)
forms[[1]][c("action", "method")]
forms[[1]]$fields[, c("name", "type", "value", "checked")]
forms[[1]]$fields$options[[2]]
forms[[2]]$fields$name
}
\seealso{
Other extraction:
\code{\link[=html_links]{html_links()}},
\code{\link[=html_list]{html_list()}},
\code{\link[=html_table]{html_table()}},
\code{\link[=html_table_cells]{html_table_cells()}},
\code{\link[=html_url]{html_url()}}
}
\concept{extraction}

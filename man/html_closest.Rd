% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{html_closest}
\alias{html_closest}
\title{Nearest ancestor matching a selector}
\usage{
html_closest(x, css)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{css}{A CSS selector: a single string.}
}
\value{
A \code{zuhtml_nodeset} as long as \code{x}: for each node, the matching
element, or a missing node where there is none (and for missing nodes).
}
\description{
For each node, the node itself if it matches \code{css}, otherwise its nearest
ancestor element that does, as the DOM's \code{closest()} and jQuery's
\code{.closest()}. Use it to go from a cell or a link to the row, card or
section that contains it. \verb{:scope} is the node itself.
}
\examples{
doc <- html_parse(paste0(
  "<table><tr id=a><td>1<td><b>x</b></tr>",
  "<tr id=b><td>2<td><b>y</b></tr></table>"
))
bold <- html_elements(doc, "b")
html_attr(html_closest(bold, "tr"), "id")
html_closest(bold, "ul")
}
\seealso{
Other navigation:
\code{\link[=html_children]{html_children()}},
\code{\link[=html_elements]{html_elements()}}
}
\concept{navigation}

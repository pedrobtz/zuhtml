% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{html_text}
\alias{html_text}
\title{Text content of nodes}
\usage{
html_text(x, recursive = TRUE)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{recursive}{If \code{FALSE}, only the direct text children of each node.}
}
\value{
A character vector as long as \code{x}. An element with no text is
\code{""}; a text, comment or processing-instruction node is its own
content; a doctype and a missing node are \code{NA}.
}
\description{
\code{html_text()} is the structural text of each node: the text nodes in its
subtree, concatenated in tree order, exactly as parsed. It inserts no
separators, trims nothing and keeps \verb{<script>} and \verb{<style>} text; it
does not include comments or the contents of \verb{<template>} elements.
}
\examples{
doc <- html_parse("<p>Hello <b>big</b> world</p>")
p <- html_children(html_children(html_root(doc))[2])
html_text(p)
html_text(p, recursive = FALSE)
}
\seealso{
Other node values:
\code{\link[=html_attr]{html_attr()}},
\code{\link[=html_markdown]{html_markdown()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_serialize]{html_serialize()}},
\code{\link[=html_strings]{html_strings()}},
\code{\link[=html_text_clean]{html_text_clean()}}
}
\concept{node values}

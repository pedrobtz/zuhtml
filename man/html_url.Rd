% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/links.R
\name{html_url}
\alias{html_url}
\title{Resolve URLs in attributes}
\usage{
html_url(x, attr = "href", base_url = NULL)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{attr}{The attribute holding the URL.}

\item{base_url}{The base URL to resolve against, overriding the
document's; \code{NULL} to use the document's.}
}
\value{
A character vector as long as \code{x}: the resolved URL of each
node. \code{NA} where the node lacks the attribute, the reference is
malformed, or a relative reference has no absolute base URL to resolve
against; and for missing nodes. The original attribute value stays
available through \code{\link[=html_attr]{html_attr()}}.
}
\description{
Reads a URL-valued attribute from each node and resolves it against the
document's base URL with the reference-resolution algorithm of RFC 3986,
section 5 (\url{https://www.rfc-editor.org/rfc/rfc3986#section-5}): dot
segments are removed and relative, query-only, fragment-only and
protocol-relative references are handled. Nothing is fetched.
}
\details{
The base URL is \code{base_url} if given; otherwise the document's first
\verb{<base href>} resolved against the \code{base_url} given to \code{\link[=html_parse]{html_parse()}};
otherwise that \code{base_url} alone.

This is RFC 3986, not the WHATWG URL Standard browsers implement: there
is no IDNA processing, no percent-encoding of characters that need it,
and no special handling of backslashes. Leading and trailing ASCII
whitespace is removed from the attribute; a reference that still
contains whitespace or a control character, or whose scheme is invalid,
is malformed.
}
\examples{
doc <- html_parse(
  "<a href='../img/a.png'>A</a><a href='//cdn.example.org/b'>B</a>",
  base_url = "https://example.org/docs/page.html"
)
html_url(html_elements(doc, "a"))
html_url(html_elements(doc, "a"), base_url = "http://other.test/x/")
}
\seealso{
Other extraction:
\code{\link[=html_links]{html_links()}},
\code{\link[=html_list]{html_list()}},
\code{\link[=html_table]{html_table()}}
}
\concept{extraction}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{html_text_clean}
\alias{html_text_clean}
\title{Cleaned text for extraction}
\usage{
html_text_clean(x, trim = TRUE, nbsp = TRUE)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{trim}{If \code{TRUE}, no whitespace at the start or end. If \code{FALSE},
whitespace there is kept, collapsed to a single space.}

\item{nbsp}{If \code{TRUE}, treat non-breaking spaces (U+00A0) as whitespace,
so they collapse like spaces; inside \verb{<pre>} they become spaces.}
}
\value{
A character vector as long as \code{x}: the cleaned text of each
element, document, fragment or text node; \code{NA} for other nodes and for
missing nodes.
}
\description{
Text as a reader would want it from a page, rather than exactly as
parsed. It is not a browser's \code{innerText}: there is no layout or CSS, and
hidden elements are included. The rules are fixed and documented:
\itemize{
\item text inside \verb{<script>}, \verb{<style>} and \verb{<template>} is skipped, and so
are comments;
\item runs of whitespace (space, tab, newline, carriage return, form feed)
collapse to a single space, except inside \verb{<pre>}, \verb{<textarea>},
\verb{<listing>} and \verb{<plaintext>}, where whitespace is kept;
\item \verb{<br>} is a line break; each block element is a line break before and
after, and adjacent block boundaries give a single line break. The
block elements are \code{address}, \code{article}, \code{aside}, \code{blockquote}, \code{body},
\code{caption}, \code{center}, \code{dd}, \code{details}, \code{dialog}, \code{dir}, \code{div}, \code{dl},
\code{dt}, \code{fieldset}, \code{figcaption}, \code{figure}, \code{footer}, \code{form}, \code{h1} to
\code{h6}, \code{head}, \code{header}, \code{hgroup}, \code{hr}, \code{html}, \code{legend}, \code{li},
\code{listing}, \code{main}, \code{menu}, \code{nav}, \code{ol}, \code{optgroup}, \code{option}, \code{p},
\code{plaintext}, \code{pre}, \code{section}, \code{summary}, \code{table}, \code{tbody}, \code{tfoot},
\code{thead}, \code{title}, \code{tr}, \code{ul} and \code{xmp};
\item table cells (\verb{<td>}, \verb{<th>}) are separated by a space.
}
}
\examples{
doc <- html_parse(paste0(
  "<div><h1>Title</h1>\n  <p>Some   <b>bold</b> text.<br>New line",
  "<script>ignored()</script></p><pre>  kept\n  as is</pre></div>"
))
div <- html_element(doc, "div")
cat(html_text_clean(div))
html_text(div)
}
\seealso{
\code{\link[=html_text]{html_text()}} for the text exactly as parsed.

Other node values:
\code{\link[=html_attr]{html_attr()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_serialize]{html_serialize()}},
\code{\link[=html_text]{html_text()}}
}
\concept{node values}

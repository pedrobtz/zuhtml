% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node.R
\name{html_children}
\alias{html_children}
\alias{html_parent}
\alias{html_ancestors}
\alias{html_next_sibling}
\alias{html_previous_sibling}
\alias{html_template_content}
\alias{html_root}
\alias{html_document}
\title{Navigate a document tree}
\usage{
html_children(x, elements_only = TRUE)

html_parent(x)

html_ancestors(x)

html_next_sibling(x, elements_only = TRUE)

html_previous_sibling(x, elements_only = TRUE)

html_template_content(x)

html_root(x)

html_document(x)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}. A document stands for
its document node.}

\item{elements_only}{If \code{TRUE}, consider element nodes only, skipping
text, comments and other nodes.}
}
\value{
\itemize{
\item \code{html_children()}, \code{html_ancestors()}, \code{html_template_content()}: a
\code{zuhtml_nodeset} in document order. \code{html_ancestors()} does not include
the document node.
\item \code{html_parent()}, \code{html_next_sibling()}, \code{html_previous_sibling()}: a
\code{zuhtml_nodeset} as long as \code{x}, with missing nodes where there is no
parent or sibling.
\item \code{html_root()}: a \code{zuhtml_nodeset} of length one: the \verb{<html>} element
of a document, or the fragment node of a fragment.
\item \code{html_document()}: the \code{zuhtml_document} that owns \code{x}.
}
}
\description{
Move from nodes to related nodes. Operations that return exactly one
node per input node -- \code{html_parent()}, \code{html_next_sibling()} and
\code{html_previous_sibling()} -- are \emph{aligned}: the result has the same
length as \code{x}, with a missing node where there is no answer, so that
results line up with their inputs. Operations that can return many nodes
per input -- \code{html_children()}, \code{html_ancestors()} and
\code{html_template_content()} -- return the set of all results, without
duplicates, in document order.
}
\details{
A \verb{<template>} element's contents are its children here, but they are
inert: searches and text extraction do not descend into them.
\code{html_template_content()} returns them explicitly.
}
\examples{
doc <- html_parse("<ul><li>One</li><li>Two <b>bold</b></li></ul>")
body <- html_children(html_root(doc))[2]
items <- html_children(html_children(body))
items
html_parent(items)
html_next_sibling(items)
html_ancestors(html_children(items[2]))

# Template contents are reached explicitly:
tmpl <- html_parse("<template><p>inert</p></template>")
html_template_content(html_children(html_children(html_root(tmpl))[1]))
}
\seealso{
Other navigation:
\code{\link[=html_closest]{html_closest()}},
\code{\link[=html_elements]{html_elements()}}
}
\concept{navigation}

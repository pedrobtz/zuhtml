% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{html_microdata}
\alias{html_microdata}
\title{Microdata items}
\usage{
html_microdata(x)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}
}
\value{
A list of items. Each item is a list with \code{type} (the tokens of
\code{itemtype}, a character vector, possibly empty), \code{id} (\code{itemid}
resolved as a URL, \code{NA} when absent or without \code{itemtype}) and
\code{properties}: a named list, in document order of first appearance, of
lists of values, since a name may occur more than once. A value is a
string or a nested item.
}
\description{
The top-level microdata items (elements with \code{itemscope} and no
\code{itemprop}) below the given nodes (and the nodes themselves), in document
order, with their properties collected by the HTML standard's algorithm
(\url{https://html.spec.whatwg.org/multipage/microdata.html}), including
properties pulled in by \code{itemref}. RDFa is not read.
}
\details{
Each property's value follows the standard: a nested item for an element
with \code{itemscope}; the \code{content} attribute of \verb{<meta>}; the resolved
\code{src} of \verb{<audio>}, \verb{<embed>}, \verb{<iframe>}, \verb{<img>}, \verb{<source>}, \verb{<track>}
and \verb{<video>}, \code{href} of \verb{<a>}, \verb{<area>} and \verb{<link>}, and \code{data} of
\verb{<object>} (resolved as \code{\link[=html_url]{html_url()}} does, \code{""} when that fails); the
\code{value} attribute of \verb{<data>} and \verb{<meter>}; the \code{datetime} attribute of
\verb{<time>} when present. Otherwise it is the element's text, which here is
cleaned as \code{\link[=html_text_clean]{html_text_clean()}} does, not the raw \code{textContent}. An item
that is its own ancestor through \code{itemref} is \code{NULL}.
}
\examples{
doc <- html_parse(paste0(
  "<div itemscope itemtype='https://schema.org/Book'>",
  "<span itemprop='name'>Dune</span>",
  "<div itemprop='author' itemscope itemtype='https://schema.org/Person'>",
  "<span itemprop='name'>Frank Herbert</span></div>",
  "<meta itemprop='isbn' content='9780441013593'>",
  "</div>"
))
book <- html_microdata(doc)[[1]]
book$type
book$properties$name[[1]]
book$properties$author[[1]]$properties$name[[1]]
}
\seealso{
Other metadata:
\code{\link[=html_json_ld]{html_json_ld()}},
\code{\link[=html_meta]{html_meta()}},
\code{\link[=html_title]{html_title()}}
}
\concept{metadata}

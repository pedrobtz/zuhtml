% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{zuhtml-conditions}
\alias{zuhtml-conditions}
\title{Conditions raised by zuhtml}
\description{
Every error zuhtml raises is a condition of class \code{zuhtml_error} with one
more specific subclass. Handle them by class, for example with
\code{tryCatch(..., zuhtml_limit_error = function(e) ...)}, never by matching
the message text, which may change.
}
\details{
\itemize{
\item \code{zuhtml_input_error}: an argument is invalid, for example \code{x} is not a
single string or raw vector, a file does not exist, a URL or connection
cannot be opened or read, or a limit is not a whole number in range. Field \code{arg} names the argument.
\item \code{zuhtml_encoding_error}: raw input cannot be decoded: an unknown
encoding, a byte sequence invalid in the declared encoding, or a
byte-order mark that contradicts \code{encoding}. Field \code{encoding}.
\item \code{zuhtml_limit_error}: a resource limit from \code{\link[=html_limits]{html_limits()}} was
exceeded. Fields \code{limit} (its name, such as \code{"max_depth"}), \code{maximum}
(its value) and \code{observed} (the value that tripped it, where known).
Nothing is returned and all native memory is released.
\item \code{zuhtml_parse_error}: an internal invariant of the parser failed. This
is a bug; please report it.
\item \code{zuhtml_pointer_error}: a document is no longer available, for example
because it was restored from a saved R session. Parse the HTML again.
}

Recoverable HTML errors, such as a missing end tag, are not conditions:
they are repaired as a browser would repair them and recorded for
\code{\link[=html_problems]{html_problems()}}.
}

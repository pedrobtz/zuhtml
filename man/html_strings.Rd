% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{html_strings}
\alias{html_strings}
\title{Text pieces of nodes}
\usage{
html_strings(x, trim = FALSE, drop_empty = FALSE)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}

\item{trim}{If \code{TRUE}, remove leading and trailing whitespace from each
piece.}

\item{drop_empty}{If \code{TRUE}, drop pieces that are empty (after trimming,
when \code{trim = TRUE}).}
}
\value{
A list as long as \code{x} of character vectors; \code{NA_character_} for
a missing node. A text node is its own single piece.
}
\description{
The text nodes of each node's subtree, in tree order, as separate
strings: the pieces \code{\link[=html_text]{html_text()}} concatenates. Boundaries between
elements are kept, which matters when the markup, not whitespace,
separates values (\verb{<td>1</td><td>2</td>} is \code{"1"}, \code{"2"}, not \code{"12"}).
Text inside \verb{<script>}, \verb{<style>} and \verb{<template>} is skipped, as are
comments.
}
\examples{
doc <- html_parse("<p>One <b>two</b>\n  <i> three </i></p>")
p <- html_element(doc, "p")
html_strings(p)
html_strings(p, trim = TRUE, drop_empty = TRUE)
}
\seealso{
Other node values:
\code{\link[=html_attr]{html_attr()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_serialize]{html_serialize()}},
\code{\link[=html_text]{html_text()}},
\code{\link[=html_text_clean]{html_text_clean()}}
}
\concept{node values}

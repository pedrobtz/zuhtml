% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markdown.R
\name{html_markdown}
\alias{html_markdown}
\title{Convert HTML to Markdown}
\usage{
html_markdown(x)
}
\arguments{
\item{x}{A \code{zuhtml_document} or \code{zuhtml_nodeset}.}
}
\value{
A character vector as long as \code{x}: the Markdown of each element,
document, fragment or text node, without a trailing newline; \code{NA} for
other nodes and for missing nodes.
}
\description{
Writes each node as CommonMark text, for reading, for notes, or as input
to a language model. Text is skipped and collapsed as
\code{\link[=html_text_clean]{html_text_clean()}} does, and Markdown-significant characters in it are
escaped, so the text reads back as written.
}
\details{
The conversion covers:
\itemize{
\item headings (\verb{#} to \verb{######}), paragraphs and the other block elements
(separated by blank lines), and thematic breaks (\verb{<hr>}, as \verb{---});
\item emphasis (\verb{<em>}, \verb{<i>}) as \verb{*...*} and strong (\verb{<strong>}, \verb{<b>}) as
\verb{**...**}, and inline code (\verb{<code>}, \verb{<kbd>}, \verb{<samp>}, \verb{<tt>}) as a
code span;
\item \verb{<pre>} as a fenced code block, with its language when a
\verb{language-*} or \verb{lang-*} class names one;
\item block quotes, and ordered and unordered lists, nested, with \code{start},
\code{reversed} and \code{value} honored; empty list items are left out;
\item links and images, their URLs resolved against the document's base URL
as \code{\link[=html_url]{html_url()}} does, or as written where that is not possible; \verb{<a>}
without \code{href} is plain text, and \verb{<img>} without \code{src} is left out;
\item \verb{<br>} as a hard line break;
\item data tables as GFM pipe tables: tables in which no cell spans rows or
columns and every cell holds only inline content (no paragraphs,
lists, headings, \verb{<div>}s, code blocks or tables). The first row is
the header row, and cell contents stay on one line. Other tables,
such as tables used for page layout, are written as their content,
one paragraph per row, with cells separated by spaces.
}

\verb{<head>}, \verb{<script>}, \verb{<style>}, \verb{<template>}, \verb{<iframe>}, \verb{<svg>} and
comments are left out. Other elements contribute their text only. Markup
that crosses block boundaries, such as \verb{<b>} around two paragraphs, is
closed and reopened in each block.
}
\examples{
doc <- html_parse(paste0(
  "<h1>Release notes</h1>",
  "<p>Version <b>2.0</b> adds <code>fetch()</code>. ",
  "See <a href='changes.html'>the changes</a>.</p>",
  "<ul><li>Faster parsing</li><li>New <i>options</i>:",
  "<ol><li>limits</li><li>encoding</li></ol></li></ul>",
  "<table><tr><th>Item</th><th>Cost</th></tr>",
  "<tr><td>Tea</td><td>3</td></tr></table>"
), base_url = "https://example.org/docs/")
cat(html_markdown(doc))
}
\seealso{
\code{\link[=html_text_clean]{html_text_clean()}} for plain text.

Other node values:
\code{\link[=html_attr]{html_attr()}},
\code{\link[=html_name]{html_name()}},
\code{\link[=html_serialize]{html_serialize()}},
\code{\link[=html_strings]{html_strings()}},
\code{\link[=html_text]{html_text()}},
\code{\link[=html_text_clean]{html_text_clean()}}
}
\concept{node values}
